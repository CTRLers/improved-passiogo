<h1>Routes</h1>

<ul>
  <% @routes.each do |route| %>
    <li><strong><%= route['name'] %></strong>
      located at: Lat: <%= route['latitude'] %>, Long: <%= route['longitude'] %></li>
  <% end %>
</ul>
